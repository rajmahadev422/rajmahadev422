name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Website
        run: |
          curl https://campus-hg1f.onrender.com
