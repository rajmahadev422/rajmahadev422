name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Campus Website
        run: |
          curl https://campus-hg1f.onrender.com
          
      - name: Ping CPP Compiler
        run: |
          curl https://compiler-kalm.onrender.com
